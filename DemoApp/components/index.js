'use strict';

/**
 * DemoApp — Components Barrel
 *
 * Export all reusable app-level components:
 *   const { ProductCard, SearchBar } = require('./components');
 */

// Uncomment and add your components as you create them:
// const { ProductCard } = require('./ProductCard');
// const { SearchBar }   = require('./SearchBar');
// const { NavBar }      = require('./NavBar');

module.exports = {
    // ProductCard,
    // SearchBar,
    // NavBar,
};

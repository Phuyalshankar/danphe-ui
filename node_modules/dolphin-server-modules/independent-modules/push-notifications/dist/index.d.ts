export interface FCMConfig {
    clientEmail: string;
    privateKey: string;
    projectId: string;
}
export interface APNsConfig {
    teamId: string;
    keyId: string;
    privateKey: string;
    sandbox?: boolean;
}
export declare class PushNotificationService {
    private fcmConfig?;
    private apnsConfig?;
    private fcmAccessToken?;
    private fcmTokenExpiry;
    private http2Connect;
    constructor(config: {
        fcm?: FCMConfig;
        apns?: APNsConfig;
        http2Connect?: any;
    });
    /**
     * Send a push notification to an Android device (FCM v1).
     */
    sendFCM(deviceToken: string, notification: {
        title: string;
        body: string;
    }, data?: Record<string, string>): Promise<any>;
    private getFCMAccessToken;
    private signRS256JWT;
    /**
     * Send a push notification to an iOS device (APNs over HTTP/2).
     */
    sendAPNs(deviceToken: string, options: {
        topic: string;
        title: string;
        body: string;
        badge?: number;
        sound?: string;
        data?: Record<string, any>;
    }): Promise<void>;
    private signES256JWT;
}

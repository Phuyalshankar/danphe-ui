/**
 * Generate a new secure ECDH (P-256) Key Pair for key exchange.
 */
export declare function generateKeyPair(): Promise<CryptoKeyPair>;
/**
 * Export a Public CryptoKey to a portable Base64-encoded string (SPKI format).
 */
export declare function exportPublicKey(key: CryptoKey): Promise<string>;
/**
 * Export a Private CryptoKey to a portable Base64-encoded string (PKCS#8 format).
 */
export declare function exportPrivateKey(key: CryptoKey): Promise<string>;
/**
 * Import a portable Base64 SPKI public key string back to a CryptoKey.
 */
export declare function importPublicKey(spkiBase64: string): Promise<CryptoKey>;
/**
 * Import a portable Base64 PKCS#8 private key string back to a CryptoKey.
 */
export declare function importPrivateKey(pkcs8Base64: string): Promise<CryptoKey>;
/**
 * Derive a shared symmetric AES-GCM (256-bit) encryption key from
 * a local private key and a remote public key.
 */
export declare function deriveSharedKey(localPrivateKey: CryptoKey, remotePublicKey: CryptoKey): Promise<CryptoKey>;
/**
 * Encrypt a plaintext message using AES-GCM (256-bit) with the derived shared key.
 * Returns the Base64 encoded ciphertext and the random IV used.
 */
export declare function encrypt(plaintext: string, sharedKey: CryptoKey): Promise<{
    ciphertext: string;
    iv: string;
}>;
/**
 * Decrypt an AES-GCM encrypted message using the shared key and the IV.
 */
export declare function decrypt(ciphertextBase64: string, ivBase64: string, sharedKey: CryptoKey): Promise<string>;
